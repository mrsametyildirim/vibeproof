name: tests

# The hook claims POSIX sh portability. Claiming it and testing it are different
# things — GNU grep accepts regex that BSD grep on macOS does not, and that class
# of bug is invisible on a single platform.
#
# This is CI infrastructure. It does not change the product promise: a VibeProof
# user still installs nothing.

on:
  push:
    paths:
      - 'hooks/**'
      - 'tests/**'
      - 'evals/**'
      - '.github/workflows/tests.yml'
  pull_request:
    paths:
      - 'hooks/**'
      - 'tests/**'
      - 'evals/**'
      - '.github/workflows/tests.yml'
  workflow_dispatch:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Shell and grep in use
        run: |
          echo "sh  -> $(command -v sh)"
          grep --version 2>&1 | head -1 || true

      # Run under dash where available. bash is forgiving about constructs that
      # dash rejects, so passing under bash alone would not prove portability.
      # Results go to the job summary AND, on failure, to workflow annotations.
      # Annotations render on the run page without signing in; raw logs do not.
      # A test suite whose failures are only visible to the maintainer is a weak
      # public trust signal — and it made the first red run undiagnosable from
      # outside the repository.
      - name: Run tripwire tests
        run: |
          status=0
          out=$(mktemp)
          {
            if command -v dash >/dev/null 2>&1; then
              echo "=== dash ==="
              dash tests/tripwire/run.sh 2>&1 || status=1
            fi
            echo "=== sh ==="
            sh tests/tripwire/run.sh 2>&1 || status=1
          } > "$out"
          cat "$out"
          { echo "## \`${{ matrix.os }}\`"; echo '```'; cat "$out"; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
          if [ "$status" -ne 0 ]; then
            grep -A2 '^  FAIL' "$out" | while IFS= read -r line; do
              printf '::error title=tripwire (%s)::%s\n' "${{ matrix.os }}" "$line"
            done
            grep -E '^[0-9]+ passed' "$out" | while IFS= read -r line; do
              printf '::error title=tripwire result (%s)::%s\n' "${{ matrix.os }}" "$line"
            done
          fi
          exit $status

      # GNU-only escapes are the portability trap this matrix exists to catch: BSD
      # grep on macOS reads them as literal letters, so the pattern silently
      # matches nothing and a check quietly stops working on half the platforms.
      #
      # `|| true` matters. Under `bash -e`, grep exiting 1 for "no matches" is the
      # normal, expected case; letting that status reach the shell is how a guard
      # ends up failing precisely when it has nothing to report.
      - name: Reject GNU-only regex escapes
        run: |
          # -F, not -E. The first version of this guard used the regex
          #     grep -nE '\\[sdwSDW]'
          # which matched nothing locally and matched on both CI runners against
          # byte-identical content — a grep-dialect disagreement about what `\\`
          # means. A check against regex-dialect bugs must not itself depend on
          # regex dialect. Fixed strings have exactly one interpretation.
          hits=$(LC_ALL=C grep -nF -e '\s' -e '\d' -e '\w' -e '\S' -e '\D' -e '\W' \
                   hooks/tripwire.sh || true)
          if [ -n "$hits" ]; then
            printf '%s\n' "$hits"
            printf '::error title=portability (%s)::GNU-only regex escape in hooks/tripwire.sh — use [[:space:]]\n' "${{ matrix.os }}"
            exit 1
          fi
          echo "no GNU-only escapes"

      # The corpus is only worth anything if every bug keeps its control. A
      # detector tested solely against broken code passes while flagging
      # everything; the control is where precision is measured. Losing one to a
      # rename should turn the build red, not quietly weaken the benchmark.
      - name: Check the eval corpus
        run: sh evals/run-check.sh

      # The README has stated the wrong reference-file count three times: it said
      # six when there were seven, seven when there were fourteen, and eighteen
      # when there were twenty-two. VibeProof found the first one in itself. A
      # number a human maintains by hand drifts; a number CI checks does not.
      - name: README reference count is true
        run: |
          actual=$(ls skills/vibeproof/references/*.md | wc -l | tr -d ' ')
          claimed=$(sed -n 's/.*plus \([0-9][0-9]*\) reference files.*//p' README.md | head -1)
          echo "claimed=$claimed actual=$actual"
          if [ "$claimed" != "$actual" ]; then
            printf '::error title=README::claims %s reference files, there are %s
' "$claimed" "$actual"
            exit 1
          fi
